#!/bin/bash

FILES=`find . -name '*.py' \
    ! -name '*_test.py' \
    ! -name 'AT*.py' \
    ! -name '__init__.py' \
    ! -name 'fixpath.py' \
    ! -name 'test_all.py' \
`

if [ -z "$FILES" ]
then
    exit 1
fi
# echo $FILES

pylint --rcfile=pylintrc $@ $FILES

